[Unit]
Description=Run the vault-keeper repo->Projects backfill each evening (after the 19:00 daily writer)

[Timer]
OnCalendar=*-*-* 19:30:00
# run after a missed window (WSL was down at 19:30) instead of skipping the day
Persistent=true

[Install]
WantedBy=timers.target
